def extract_user_details(user_input):
    # A simple placeholder to extract travel details.
    # In a real application, use NLP methods or an AI model to parse details.
    # For now, we assume a fixed output based on the example input.
    return {
        "destination": "Paris",
        "days": 5,
        "budget": "moderate",
        "interests": "history and art"
    }
